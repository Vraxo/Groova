This is preventing me from switching click manager
from Ready to Start, which would remove the need for
checking if the clickable is active, which is there
because deactivated unshown items in ItemLists never
run their Ready method and therefore don't get their
click manager.

Make padding in button style 
to text origin for textbox

Add a way to access window resolution 
and delta time without raylib

Screenshots are still being loaded



- - - - - - - - - - Long Term - - - - - - - - - -



* when left & right click are both enabled on a button,
  the button only changes color on pressed on right clicks.


* horizontal slider button is one pixel high

* make on update a universal thing

* make horizontal slider work with origins

* sync slider button with actual value